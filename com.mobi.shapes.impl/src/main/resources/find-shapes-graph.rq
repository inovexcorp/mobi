PREFIX mcat: <http://mobi.com/ontologies/catalog#>
PREFIX shapesedit: <http://mobi.com/ontologies/shapes-graph-editor#>

SELECT ?record
WHERE {
	?record a shapesedit:ShapesGraphRecord ;
        mcat:catalog ?catalog ;
		shapesedit:shapesGraphIRI ?shapesGraphIRI .
}
